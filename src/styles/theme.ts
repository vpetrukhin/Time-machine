import {createTheme} from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#311b92',
            light: '#311b92',
            contrastText: '#fff',
        },
        secondary: {
            main: '#000000'
        }
    },
});